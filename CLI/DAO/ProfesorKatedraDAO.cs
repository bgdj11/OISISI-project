using CLI.Model;
using CLI.Serialization;
using CLI.Storage;

namespace CLI.DAO
{
    class ProfesorKatedraDAO
    {   
        // private readonly List<ProfesorKatedra> _profesorKatedraList;
       // private readonly Storage<ProfesorKatedra> _storage;
        public ProfesorKatedraDAO(ProfesorDAO _profesor, KatedraDAO _katedra)
        {
           
        }
    }
}
